import 'package:flutter/material.dart';

class InitializePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Initialize'),
      ),
      body: Center(child: Text('Initialize Page')),
    );
  }
}
